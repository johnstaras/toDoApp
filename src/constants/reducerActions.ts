export enum ReducerActions {
  ADD = 'add',
  CHANGE = 'change',
  DELETE = 'delete',
  SET_ITEMS = 'set_items',
  CHANGE_FILTER = 'change_filter',
  SET_ERROR = 'set_error',
  SET_LOADER = 'set_loader',
}
