import {TextField} from '@mui/material';

const SearchBox = () => {
  return (
    
      <TextField
        type='text'
        placeholder={'I am looking for...'}
        style={{width: '100%'}}
      />
    
  );
};

export default SearchBox;
